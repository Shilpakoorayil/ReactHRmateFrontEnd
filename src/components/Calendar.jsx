import React from 'react'
export default function Calendar(){ 
  return <div style={{minHeight:180,display:'flex',alignItems:'center',justifyContent:'center',color:'#666'}}>Calendar (FullCalendar placeholder) - install deps to enable</div>
}
